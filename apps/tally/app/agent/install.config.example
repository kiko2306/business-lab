<?xml version="1.0" encoding="utf-8"?>
<!-- Optional answers for the setup exe. Save as install.config in the same
     folder as the setup exe: a value filled in here is not asked for, an empty
     one is prompted. The enrolment code is single-use and expires in minutes,
     so it is normally left empty. -->
<tallyInstall>
  <url value="" />
  <code value="" />
  <wintouchDir value="" />
</tallyInstall>
