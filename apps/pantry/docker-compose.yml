services:
  pantry:
    build: ./app
    restart: unless-stopped
    ports:
      - "${PANTRY_PORT:-10300}:3000"
    environment:
      TZ: ${TZ:-UTC}
    volumes:
      # Single items.json file — this app deliberately has no real database,
      # see app/server.js. Small dataset, single-user, low write concurrency.
      - ./data:/data
    labels:
      - "homepage.group=Productivity"
      - "homepage.name=Pantry"
      # Custom app, no upstream icon — its own logo, served from Homepage's
      # config/icons/ (a copy of app/public/favicon.svg). §131.1.
      - "homepage.icon=/icons/pantry.svg"
      - "homepage.href=http://localhost:${PANTRY_PORT:-10300}"
      - "homepage.description=Custom pantry stock tracker (expiration dates, use-item flow, shopping list)"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 10s
