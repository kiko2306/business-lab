
services:
  nocodb:
    # 768m OOM-looped this Node/Nest app on the §305 rollout (V8 GC thrash
    # under a cgroup cap set from idle RSS ~570 MiB). 1200m holds.
    mem_limit: 1200m
    mem_reservation: 256m
    image: nocodb/nocodb:latest
    restart: unless-stopped
    ports:
      - "${NOCODB_PORT:-10280}:8080"
    environment:
      # NocoDB's own metadata DB. Keep NOCODB_DB_PASSWORD alphanumeric — it is
      # substituted into this URL-style connection string.
      NC_DB: "pg://nocodb-db:5432?u=${NOCODB_DB_USER:-nocodb}&p=${NOCODB_DB_PASSWORD}&d=${NOCODB_DB_NAME:-nocodb}"
      # Signs auth tokens. No default — the dashboard's Configuration panel
      # pre-fills a generated value; a per-start random one would drop every
      # session on restart.
      NC_AUTH_JWT_SECRET: ${NOCODB_JWT_SECRET}
      # Builds invite links, shared-view URLs and email links — the dashboard
      # rewrites it to the public https:// hostname once exposure is enabled.
      NC_PUBLIC_URL: ${NOCODB_PUBLIC_URL:-http://localhost:8011}
      NC_DISABLE_TELE: ${NOCODB_DISABLE_TELEMETRY:-true}
      # Super admin, (re-)provisioned from these on every boot (§344). The
      # dashboard injects NC_ADMIN_EMAIL (the Authelia admin's) at start and
      # generates NC_ADMIN_PASSWORD into .env — NocoDB has no OIDC in the
      # community edition, so it's exposed directly with its own login as the
      # only gate. NocoDB requires both to be set together.
      NC_ADMIN_EMAIL: ${NOCODB_ADMIN_EMAIL:-}
      NC_ADMIN_PASSWORD: ${NOCODB_ADMIN_PASSWORD:-}
      TZ: ${TZ:-UTC}
    labels:
      - "homepage.group=Data"
      - "homepage.name=NocoDB"
      - "homepage.icon=nocodb.png"
      - "homepage.href=http://localhost:${NOCODB_PORT:-10280}"
      - "homepage.description=Airtable-style database over Postgres"
    volumes:
      - ./data/nocodb:/usr/app/data
    depends_on:
      nocodb-db:
        condition: service_healthy
    networks:
      - nocodb-net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # NocoDB officially supports Postgres up to 16; newer majors aren't
  # validated by upstream yet, so this isn't a stale pin.
  nocodb-db:
    mem_limit: 256m
    mem_reservation: 64m
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${NOCODB_DB_NAME:-nocodb}
      POSTGRES_USER: ${NOCODB_DB_USER:-nocodb}
      POSTGRES_PASSWORD: ${NOCODB_DB_PASSWORD}
    volumes:
      - ./data/db:/var/lib/postgresql/data
    networks:
      - nocodb-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${NOCODB_DB_USER:-nocodb}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  nocodb-net:
    driver: bridge
