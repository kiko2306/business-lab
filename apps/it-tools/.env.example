# Copy to .env in this directory and set secure values before starting the app.
# Docker Compose loads this file automatically from the app's project directory.

IT_TOOLS_PORT=10490

# IT-Tools is a fully static SPA — no backend, no data, no config, no login
# of its own. Exposure needs no extra settings; Authelia is the only gate.
